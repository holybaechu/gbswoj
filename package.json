{
  "name": "gbswoj",
  "description": "Gyeongbuk Software Meister High School Online Judge",
  "private": true,
  "scripts": {
    "dev": "docker compose up -d redis && turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "turbo run format"
  },
  "devDependencies": {
    "turbo": "^2.9.3"
  },
  "packageManager": "bun@1.3.11",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
